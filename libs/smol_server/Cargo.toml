[package]
name = "smol_server"
version = "0.1.0"
edition = "2024"

[dependencies]
http_parser = { version = "0.1.0", path = "../http_parser" }
matchit = "0.8.6"
tcp_wrapper = { version = "0.1.0", path = "../tcp_wrapper" }
